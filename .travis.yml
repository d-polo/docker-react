language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t riskipolo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true riskipolo/docker-react npm run test
